{
  "name": "cogito",
  "private": true,
  "scripts": {
    "build": "pnpm --prefix apps/website install && pnpm --prefix apps/website run build && rm -rf out && cp -r apps/website/out .",
    "dev:website": "pnpm --prefix apps/website dev"
  }
}
