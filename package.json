{
  "name": "cogito",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps/website install && npm --prefix apps/website run build && rm -rf out && cp -r apps/website/out .",
    "dev:website": "pnpm --prefix apps/website dev"
  }
}
